# Easier navigation
alias ..="cd.."

# Quick access to my projects. Can be used like
# $ work
# to simply navigate to work directory
# or
# $ work web
# to navigate to the web directory in 'work'
work () { cd ~/dev/work/$@ fixed args; }
personal () { cd ~/dev/personal/$@ fixed args; }