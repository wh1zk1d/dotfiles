# Easier navigation
alias ..="cd.."

# Quick access to my projects. Can be used like
# $ work
alias work="cd ~/dev/work"
alias personal="cd ~/dev/personal"

# Get local IP
alias local-ip="ipconfig getifaddr en0"